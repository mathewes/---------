代码说明：

1. 应该我的电脑上没有vs, 所以只在gcc下进行了测试

2. 在vs下（推荐vs2005以上版本）， 将code.c拷入代码目录。将config.ini放入合适位置（具体不清楚应该是哪一个文件夹）。

3. 题意理解

	3.1 本文所谓的逼近法，就是3点确定一个圆。要求是一定区间中的点到圆的距离小于一个delta。

	3.2 本文采用的流程就是第二页上面的流程图

4. 输入文件格式，输出文件格式参考样例(config.ini, out.ini)
	
6. 特殊情况的处理

6.1 在最后剩余的点数不足四个的时候，不考虑剩下的点

6.2 在step为4的时候，如果也不满足则以首尾为直径。使用getSpecailCircle函数获得

7. 存在的问题

7.1 最后获得的结果和示例不一样. 但是考虑到是逼近算法，现有结果也可接受

7.2 没有处理误差的正负（讨论情况增加即可）
